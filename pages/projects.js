import Layout from '../components/Layout';

const projectsPage = (
    <p>This is project page</p>
);

//TODO: add (possibly) cards with images + summary
export default function Projects() {
    return <Layout content={projectsPage} />;
}