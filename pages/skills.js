import Layout from '../components/Layout';

const skillsPage = (
    <p>This is project page</p>
);

//TODO: add (possibly) cards with images + summary
export default function Skills() {
    return <Layout content={skillsPage} />;
}