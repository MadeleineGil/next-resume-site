import Layout from '../components/Layout';

const workPage = (
    <p>This is work page</p>
);

// TODO: find a nice way to present this
export default function Work() {
    return <Layout content={workPage} />;
}